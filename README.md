# Student-Registration-System
A C# Windows Forms application connected to SQL Server for managing student records. It includes secure login and basic CRUD operations (add, edit, delete, search). The system provides a simple user-friendly interface with report generation from live data. Built using C# (.NET), WinForms, SQL Server, and ADO.NET.
